# Clipor (ja)

## セキュリティに関する注意事項

- クリップボード履歴はローカルのSQLiteデータベース（`%LOCALAPPDATA%\Clipor\history.db`）に**暗号化されずに**保存されます。パスワードやAPIキーなどの機密情報がクリップボードにコピーされた場合、履歴に残る可能性があります。
- 本アプリケーションは外部サーバーへの通信を一切行いません。すべてのデータはローカルに保存・処理されます。
- Windows レジストリへの書き込みは「スタートアップ起動」設定時のみ、`HKEY_CURRENT_USER` に対して行われます。
